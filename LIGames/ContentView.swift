import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Hello, welcome to LI Games")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
